name: snippy-env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # Note: Snippy 4.6.0 has compatibility issues on ARM64.
  # Consider using Docker with --use-singularity or containerization instead.
  - samtools
  - bcftools
  - seqtk
  - parallel
  - bedtools
